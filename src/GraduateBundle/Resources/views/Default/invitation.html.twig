<html>
 <head>
  <title>Invitados {{graduate.nombre}} {{graduate.apellido}}</title>
    <style>

body {
  text-align: center;
  font-family: sans-serif;
  font-size: 0.7rem;
  color:#5f5f5f ;
}

.card {
  background: #fff;
  border-radius: 2px;
  display: inline-block;
  height: 260px;
  margin: 1rem;
  margin-bottom:2rem;
  position: relative;
  width: 100%; 
  display: flex;
   border: 1px solid #a0a0a0;
	box-shadow: 0 8px 17px 0 rgba(0, 0, 0, 0.6), 0 6px 20px 0 rgba(0, 0, 0, 0.6);
    -webkit-box-shadow: 0 8px 17px 0 rgba(0, 0, 0, 0.6), 0 6px 20px 0 rgba(0, 0, 0, 0.6);
    -moz-box-shadow: 0 8px 17px 0 rgba(0, 0, 0, 0.6), 0 6px 20px 0 rgba(0, 0, 0, 0.6);
}

.cut{ 
	 margin-bottom:1.5rem;
}

.qr{
	float: left;
}

img{
  max-width: 100%;
  height: auto;
}

.left{
	float:left; 
	width:50%;
	background: trasparent;
	}
	
	.info{
	 border: 1px dashed #d11c23;
	box-shadow: 0 0 0 3px #fff, 0 0 0 5px #d11c23, 0 0 0 10px #fff, 0 0 2px 10px #d11c23;
	height: auto;
	width:93%;
	}
	#norte{
	 padding-top: 1rem;
	}

	.importante{
	 color:#d11c23 ;
	 font-weight:bolder;
	 font-size: 0.9rem;
	}

	.espacio{
	 height: 2rem;
	}
	
           </style>
           </head>
           <body>
   
{% for key,guest in info %}

{% if key % 3 == 0  %}
<br><div class="espacio"></div>
{% endif %}

<div class="card">
<div class="left">
<img class="qr" src="{{ qrcode_data_uri(guest[0]) }}" />
</div>

<div class="left">
<br><br>
	<div class="info">
		<div id="norte"><p>Estimado Señor(a):<br/> <span class="importante">{{ guest[1][0] }} {{ guest[1][1] }}</span><br/> Nos complace invitarle a la Ceremonia Solemne de graduación de <br/><span class="importante">{{graduate.nombre}} {{graduate.apellido}}</span></p></div>
			<div id="centro"><p>Graduando del Programa Académico de<br/><span class="importante">{{graduate.programa.nombre}}<br/>{{graduate.programa.facultad.nombre}}</span></p></div>
			<br/>
<div id="sur">
<div class="left">
<p><span class="importante">{{lugar}}</span><br/>{{direccion}}</p>
</div>			
			<div class="left">
		<p><span class="importante">{{fecha_evento}}</span><br/>{{hora_evento}}</p>
			</div>
			</div>		
</div>
</div>
</div>
{% if (key != limite) and ((key+1) % 3 != 0 ) %}
<img class="cut" src="{{ asset('img/overlays/cut.png')}}"/><br>
{% endif %}
{% endfor %}

    </body>
</html>

